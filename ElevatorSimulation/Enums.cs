// Enums.cs
namespace ElevatorSimulation
{
    public enum Direction { Up, Down, Idle }
    public enum ElevatorType { Standard, HighSpeed, Glass, Freight }
    public enum ElevatorStatus { Available, OutOfService, Maintenance }
}
